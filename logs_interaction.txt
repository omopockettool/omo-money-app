Framework concerns stay in the Data layer where they belong.

Key Points for today:
Start with CategoryService (incremental, less risky)
Services will return CategoryDomain instead of Category
Cache Domain models instead of Core Data entities
Repositories become thin wrappers (no more .toDomain() calls)
All Core Data conversion happens inside Services
The document has detailed code examples for every change needed. When you resume tomorrow, just open this file and follow the steps! 🚀


Priority 4: Other Dependencies - currentGroup, currentUser, availableGroups - Still using Core Data entities

Once you complete the new Service layer refactor, you can finish the final 3 properties in DashboardViewModel (currentGroup, currentUser, availableGroups).

23-12-2025

Read UserDetailViewModel.swift
I can see that this ViewModel is heavily dependent on UserGroupService which still uses Core Data entities. The architectural violation at line 72 calling .toDomain() is a symptom of the deeper issue: UserGroupService hasn't been refactored to Pure Domain yet. Let me update the todo list to reflect that we need to fix UserGroupService FIRST before fixing UserDetailViewModel:

Update Todos

Create UserGroupDomain model DONE

Refactor UserGroupService to return UserGroupDomain DONE

Refactor UserDetailViewModel to use UserGroupDomain

Remove .toCoreData() call in DashboardViewModel test data generation

Refactor ItemService to return ItemDomain

Refactor ItemListService to return ItemListDomain

why is necesary ItemListDetailNavigationWrapper in DashBoardView ???? this has funtions 